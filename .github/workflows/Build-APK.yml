name: Build Android APK (Production Ready)

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type (debug or release)'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release
  push:
    branches:
      - main

# Production Supabase Configuration - HARDCODED for reliability
env:
  VITE_SUPABASE_URL: "https://pmqkclhqvjfmcxzuoypa.supabase.co"
  VITE_SUPABASE_PUBLISHABLE_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBtcWtjbGhxdmpmbWN4enVveXBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjY0ODY2MTEsImV4cCI6MjA4MjA2MjYxMX0.EObJ4bGppAue12-eXk-CjWTCvSaqEKRpVeObJ7O7r64"
  VITE_SUPABASE_PROJECT_ID: "pmqkclhqvjfmcxzuoypa"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ========== STEP 1: Checkout ==========
      - name: Checkout repository
        uses: actions/checkout@v4

      # ========== STEP 2: Validate Critical Files ==========
      - name: Validate google-services.json
        run: |
          echo "📱 Validating Firebase configuration..."
          
          if [ ! -f frontend/android/app/google-services.json ]; then
            echo "❌ CRITICAL: google-services.json not found!"
            echo "📍 Expected location: frontend/android/app/google-services.json"
            exit 1
          fi
          
          # Validate package name
          PACKAGE_NAME=$(cat frontend/android/app/google-services.json | grep -o '"package_name": *"[^"]*"' | head -1 | cut -d'"' -f4)
          echo "📦 Package name in google-services.json: $PACKAGE_NAME"
          
          if [ "$PACKAGE_NAME" != "com.fasthaazir.app" ]; then
            echo "❌ CRITICAL: Package name mismatch!"
            echo "   Expected: com.fasthaazir.app"
            echo "   Found: $PACKAGE_NAME"
            exit 1
          fi
          
          # Validate Firebase project ID
          PROJECT_ID=$(cat frontend/android/app/google-services.json | grep -o '"project_id": *"[^"]*"' | head -1 | cut -d'"' -f4)
          echo "🔥 Firebase Project ID: $PROJECT_ID"
          
          if [ "$PROJECT_ID" != "fast-haazir-786" ]; then
            echo "⚠️ WARNING: Firebase project ID is '$PROJECT_ID', expected 'fast-haazir-786'"
          fi
          
          echo "✅ google-services.json validated successfully!"

      # ========== STEP 3: Setup Environment ==========
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      # ========== STEP 4: Install Dependencies ==========
      - name: Install frontend dependencies
        working-directory: frontend
        run: |
          echo "📦 Installing dependencies..."
          npm ci --legacy-peer-deps || npm install --legacy-peer-deps
          echo "✅ Dependencies installed!"

      # ========== STEP 5: Create Production .env ==========
      - name: Create production environment
        working-directory: frontend
        run: |
          echo "📝 Creating production .env file..."
          cat > .env << 'EOF'
          VITE_SUPABASE_URL=https://pmqkclhqvjfmcxzuoypa.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBtcWtjbGhxdmpmbWN4enVveXBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjY0ODY2MTEsImV4cCI6MjA4MjA2MjYxMX0.EObJ4bGppAue12-eXk-CjWTCvSaqEKRpVeObJ7O7r64
          VITE_SUPABASE_PROJECT_ID=pmqkclhqvjfmcxzuoypa
          NODE_ENV=production
          EOF
          
          echo "✅ .env file created with production values"
          echo "📋 Contents:"
          cat .env

      # ========== STEP 6: Build Frontend ==========
      - name: Build frontend for production
        working-directory: frontend
        run: |
          echo "🔨 Building frontend..."
          npm run build
          echo "✅ Frontend build completed!"
        env:
          CI: false
          NODE_ENV: production

      # ========== STEP 7: Verify Build Output ==========
      - name: Verify build output
        working-directory: frontend
        run: |
          echo "🔍 Verifying build output..."
          
          if [ ! -d "dist" ]; then
            echo "❌ Build failed: dist directory not found!"
            exit 1
          fi
          
          if [ ! -f "dist/index.html" ]; then
            echo "❌ Build failed: index.html not found in dist!"
            exit 1
          fi
          
          # Check for Supabase URL in built assets
          if grep -r "pmqkclhqvjfmcxzuoypa" dist/*.js 2>/dev/null || grep -r "pmqkclhqvjfmcxzuoypa" dist/assets/*.js 2>/dev/null; then
            echo "✅ Supabase configuration found in build output"
          else
            echo "⚠️ Warning: Could not verify Supabase URL in build (may be minified)"
          fi
          
          echo "📁 Build output:"
          ls -la dist/
          echo "✅ Build verification passed!"

      # ========== STEP 8: Sync Capacitor ==========
      - name: Sync Capacitor Android
        working-directory: frontend
        run: |
          echo "🔄 Syncing Capacitor..."
          npx cap sync android
          echo "✅ Capacitor sync completed!"

      # ========== STEP 9: Verify Android Configuration ==========
      - name: Verify Android configuration
        working-directory: frontend/android
        run: |
          echo "🤖 Verifying Android configuration..."
          
          # Check gradle wrapper
          if [ ! -f "gradlew" ]; then
            echo "❌ gradlew not found!"
            exit 1
          fi
          
          # Check AndroidManifest.xml permissions
          echo "📋 Checking Android permissions..."
          if grep -q "android.permission.INTERNET" app/src/main/AndroidManifest.xml; then
            echo "  ✅ INTERNET permission present"
          else
            echo "  ❌ INTERNET permission missing!"
            exit 1
          fi
          
          if grep -q "android.permission.ACCESS_NETWORK_STATE" app/src/main/AndroidManifest.xml; then
            echo "  ✅ ACCESS_NETWORK_STATE permission present"
          else
            echo "  ⚠️ ACCESS_NETWORK_STATE permission not found"
          fi
          
          # Check network security config
          if [ -f "app/src/main/res/xml/network_security_config.xml" ]; then
            echo "  ✅ network_security_config.xml present"
            if grep -q "supabase.co" app/src/main/res/xml/network_security_config.xml; then
              echo "  ✅ Supabase domain configured in network security"
            fi
          else
            echo "  ⚠️ network_security_config.xml not found"
          fi
          
          echo "✅ Android configuration verified!"

      # ========== STEP 10: Make gradlew Executable ==========
      - name: Make gradlew executable
        working-directory: frontend/android
        run: chmod +x gradlew

      # ========== STEP 11: Build Debug APK ==========
      - name: Build Debug APK
        working-directory: frontend/android
        run: |
          echo "🔨 Building Debug APK..."
          ./gradlew assembleDebug --stacktrace --info
          echo "✅ Debug APK build completed!"

      # ========== STEP 12: Verify APK Output ==========
      - name: Verify APK output
        working-directory: frontend/android
        run: |
          echo "📱 Verifying APK output..."
          
          APK_PATH="app/build/outputs/apk/debug/app-debug.apk"
          
          if [ ! -f "$APK_PATH" ]; then
            echo "❌ APK not found at expected path!"
            echo "📁 Searching for APK files..."
            find . -name "*.apk" -type f
            exit 1
          fi
          
          APK_SIZE=$(du -h "$APK_PATH" | cut -f1)
          echo "✅ APK found: $APK_PATH"
          echo "📦 APK size: $APK_SIZE"

      # ========== STEP 13: Upload Debug APK ==========
      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: fast-haazir-debug-apk
          path: frontend/android/app/build/outputs/apk/debug/*.apk
          retention-days: 30
          if-no-files-found: error

      # ========== STEP 14: Build Summary ==========
      - name: Build Summary
        run: |
          echo ""
          echo "╔════════════════════════════════════════════════════════════════╗"
          echo "║                    🎉 BUILD SUCCESSFUL! 🎉                     ║"
          echo "╠════════════════════════════════════════════════════════════════╣"
          echo "║                                                                 ║"
          echo "║  📱 APK: fast-haazir-debug-apk                                 ║"
          echo "║  📦 Package: com.fasthaazir.app                                ║"
          echo "║  🔥 Firebase: fast-haazir-786                                  ║"
          echo "║  🗄️ Supabase: pmqkclhqvjfmcxzuoypa                              ║"
          echo "║                                                                 ║"
          echo "║  ⚠️ IMPORTANT: For Phone OTP to work, ensure SHA keys          ║"
          echo "║     are added to Firebase Console:                             ║"
          echo "║     https://console.firebase.google.com/project/fast-haazir-786║"
          echo "║                                                                 ║"
          echo "╚════════════════════════════════════════════════════════════════╝"
          echo ""
